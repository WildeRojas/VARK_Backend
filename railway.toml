[deploy]
startCommand = "sh -c 'gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 2 --timeout 120 --access-logfile -'"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
